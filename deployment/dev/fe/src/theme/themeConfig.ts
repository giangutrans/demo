import type { ThemeConfig } from "antd";

const theme_token: ThemeConfig = {
  token: {
    fontSize: 14,
    colorPrimary: "#184858",
    fontFamily: "Open Sans",
  },
};

export default theme_token;
